<section class="section welcome" id="about" aria-labelledby="welcome-title">
  <div class="container">
    <div class="welcome__panel">
      <h2 class="welcome__title" id="welcome-title">
        <span>Supporting Families</span>
        <span>Since 2010</span>
      </h2>
      <div class="welcome__body">
        <p>
          Kindercrest School specializes in nurturing the potential of each child, offering two main
          sections: The Preschool, catering to Creche, Pre Nursery, Nursery 1 &amp; 2, and the Primary
          Grade, encompassing Grade 1 to 6.
        </p>
        <p>
          Established in 2010 initially as a Creche in the tranquil setting of Kado Estate, Kindercrest
          has consistently upheld high standards, ensuring an enjoyable learning experience for all
          students.
        </p>
      </div>
    </div>
  </div>
</section>

<style>
  .welcome {
    background:
      linear-gradient(180deg, #fbfaf4 0%, #fdfbf7 58%, var(--kc-white) 100%);
  }

  .welcome__panel {
    position: relative;
    overflow: hidden;
    padding: clamp(4rem, 7vw, 6.8rem) clamp(1.5rem, 8vw, 8rem);
  }

  .welcome__panel::before {
    content: '';
    position: absolute;
    z-index: 0;
    top: 50%;
    left: 50%;
    width: min(460px, 72vw);
    aspect-ratio: 1;
    background: url('/assets/logo/kclogo.png') center / contain no-repeat;
    opacity: 0.082;
    transform: translate(-50%, -47%);
    pointer-events: none;
  }

  .welcome__panel > * {
    position: relative;
    z-index: 1;
  }

  .welcome__title {
    display: grid;
    justify-content: center;
    margin: 0;
    color: var(--kc-navy);
    font-family: var(--font-body);
    font-size: clamp(3.6rem, 7.2vw, 7.4rem);
    font-weight: 680;
    letter-spacing: 0;
    line-height: 0.9;
    text-align: center;
  }

  .welcome__title span {
    white-space: nowrap;
  }

  .welcome__body {
    width: min(720px, 100%);
    margin: clamp(2.7rem, 5vw, 4rem) auto 0;
    color: var(--kc-navy);
    font-family: var(--font-body);
    font-size: clamp(1.08rem, 1.8vw, 1.46rem);
    line-height: 1.35;
  }

  .welcome__body p {
    margin: 0;
  }

  .welcome__body p + p {
    margin-top: 1.25rem;
  }

  @media (max-width: 860px) {
    .welcome__panel {
      padding: clamp(3rem, 10vw, 4.5rem) clamp(1.75rem, 10vw, 3.5rem);
    }

    .welcome__title {
      justify-content: start;
      font-size: clamp(2.45rem, 9.6vw, 3.5rem);
      text-align: left;
    }

    .welcome__title span {
      white-space: normal;
    }

    .welcome__body {
      margin-top: 2rem;
      font-size: clamp(1.16rem, 5vw, 1.38rem);
      line-height: 1.28;
    }
  }
</style>
